/** @type {import('next').NextConfig} */
const nextConfig = {
    images:  {
        remotePatterns: [
            {hostname: "groovy-mouse-295.convex.cloud"}
        ]
    }
};

export default nextConfig;
