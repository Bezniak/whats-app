const authConfig = {
    providers: [
        {
            domain: "https://humorous-albacore-52.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;